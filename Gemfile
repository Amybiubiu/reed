source "https://rubygems.org"

# 本地开发用:GitHub Pages 原生构建时忽略此文件,使用 Pages 自己的固定环境。
# 固定 ~> 232 对应 Pages 的 Jekyll 3.10 环境,兼容本机 Ruby 2.6。
# 插件(jekyll-sitemap / jekyll-feed / jekyll-seo-tag)由该 meta-gem 自带,
# 在 _config.yml 的 plugins: 中声明即可,不要在此重复添加。
gem "github-pages", "~> 232", group: :jekyll_plugins
